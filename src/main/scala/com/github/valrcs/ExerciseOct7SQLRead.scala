package com.github.valrcs

object ExerciseOct7SQLRead {
  val dbPath = "./src/resources/db/chinook.db"
  val url = "jdbc:sqlite:"+dbPath

  //connect to database and extract ALL artist data into an Array of Artist case class

}

//so we have a singleton object meaning only a single instance of our class Utilities
object Utilities {
  //I am going to use this to store useful functions/methods for other classes and objects to use
  def add(a:Int, b:Int):Int = a+b
  //we will add some useful ones as well :)
}
